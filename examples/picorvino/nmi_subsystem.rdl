addrmap nmi_subsystem{
    name = "NMI subsystem";

    nmi_master cpu;
    // Memories
    nmi_memory #(
        .MEM_SIZE(1024),
        .DATA_WIDTH(32),
        .ADDR_WIDTH(32)
        ) bootmem;

    nmi_memory #(
        .MEM_SIZE(64000),
        .DATA_WIDTH(32),
        .ADDR_WIDTH(32)
        ) mem0;

        clk clk;
        rstn rstn;

};
